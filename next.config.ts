import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // distDir: 'dist',  // changes the build output directory (optional)
  images: {
    domains: ["ijvxvfwtpszsighgwylv.supabase.co"], // replace with the actual domain
  },
};

export default nextConfig;
